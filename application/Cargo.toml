[package]
name = "application"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
iced = {version = "0.12.1", features = ["lazy", "advanced", "canvas", "debug"]}
iced_style = "0.12.1"
rfd = {workspace = true}